version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - amplifyPush --simple
        - npm run build
        - aws s3 sync "s3://xrhk8ga-uxwxk18" .serverless   # pull what we had before
        - npx serverless                                   # generate new
        - aws s3 sync .serverless "s3://xrhk8ga-uxwxk18"   # push to bucket
frontend:
  phases:
    build:
      commands:
        - echo "Nothing to build here"
  artifacts:
    baseDirectory: .next
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*